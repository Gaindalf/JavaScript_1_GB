if (!("a" in window)) {
var a = 1;
}
alert(a); // undefined. Т.к. а - локальная переменная.



var b = function a(x) {
x && a(--x);
}
alert(a); // Ничего не случится. Чтобы вызвать метод, надо вызвать b.



function a(x) {
return x * 2;
}
var a;
alert(a); // Выведет функцию а, т.к. переменная а не определена.


function b(x, y, a) {
arguments[2] = 10;
alert(a);
}
b(1, 2, 3); // Выводит 10, т.к. а переопределяется через массив аргументов


function a() {
alert("This " + this);
}
a.call(null); // метод а вызовется для объекта null. На экране будет "This (объект)"